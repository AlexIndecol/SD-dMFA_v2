# Outputs

Model runs write results to: `outputs/<scenario_id>/<timestamp>/`

Each run includes:
- configs_snapshot/: a copy of configs used
- run_metadata.yml: run metadata
- indicators/: per-indicator CSVs (placeholder computation in dry-run)
